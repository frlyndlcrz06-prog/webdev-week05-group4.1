<?php

// autoload.php @generated by Composer // Composer-generated autoload entry point.

if (PHP_VERSION_ID < 50600) { // Stops loading if the PHP version is too old for Composer 2.3.
    if (!headers_sent()) { // Sends a server error header only if headers have not already been sent.
        header('HTTP/1.1 500 Internal Server Error'); // Returns a 500 error to browser clients.
    }
    $err = 'Composer 2.3.0 dropped support for autoloading on PHP <5.6 and you are running ' . PHP_VERSION . ', please upgrade PHP or use Composer 2.2 LTS via "composer self-update --2.2". Aborting.' . PHP_EOL; // Builds the compatibility error message.
    if (!ini_get('display_errors')) { // Hides detailed errors from the user output when display_errors is off.
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') { // Writes the message to STDERR in CLI or PHPDBG mode.
            fwrite(STDERR, $err); // Prints the compatibility error to the terminal.
        } elseif (!headers_sent()) { // Prints the error to the browser when headers are still available.
            echo $err; // Outputs the error message.
        }
    }
    throw new RuntimeException($err); // Aborts execution with a runtime exception.
}

require_once __DIR__ . '/composer/autoload_real.php'; // Loads Composer's real autoloader bootstrap.

return ComposerAutoloaderInitb62886088394d7ed0315cdb4f55da24e::getLoader(); // Returns the initialized Composer ClassLoader instance.
